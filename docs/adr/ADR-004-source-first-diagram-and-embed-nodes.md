# ADR-004 Source-first diagram and embed nodes

## Status
Accepted

## Decision
Mermaid / draw.io 采用 source-first 语义块节点。

## Rationale
- Mermaid 存源码，不只存 SVG
- draw.io 保留原始 XML，短期以嵌入 / 预览 / 导出为主
- 预览结果属于派生数据，不作为主存储格式
